[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["templates/sports-live-scores/tests"]
pythonpath = [".", "templates/sports-live-scores/src"]
addopts = "-q"
